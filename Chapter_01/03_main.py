# This is an User-defined Modules
import maths

print(maths.add(1,2))
print(maths.subtract(2,1))
print(maths.multiply(1,2))

# This is an Built-in Modules
import math

print(math.sqrt(4));

# This is an External Modules

import numpy as np

arr=np.array([1,2,3,4,5])

print(arr)

# pip install numpy
# pip uninstall numpy